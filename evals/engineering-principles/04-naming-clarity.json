{
  "prompt": "Review this function for clean code:\n```javascript\nfunction proc(d, f, x) {\n  let tmp = d.filter(i => i.v > x);\n  return f ? tmp.sort() : tmp;\n}\n```",
  "expected_signals": ["naming", "descriptive", "readable", "intent"],
  "forbidden_signals": ["performance", "algorithmic complexity"]
}
