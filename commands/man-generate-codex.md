---
description: "Generate mankit agents and skills into Codex CLI global format (~/.codex/ and ~/.agents/). Run after adding/updating skills or agents."
---

Run the Codex generator script:

```
python "$CLAUDE_PLUGIN_ROOT/scripts/generate-codex.py"
```

Report the results to the user: how many agents and skills were generated, and where the files were written.
