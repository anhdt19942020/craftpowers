---
description: "Start a tracked agentic loop with iteration checkpoint. Tracks pass/fail history and supports resume."
---

Invoke the `loop-checkpoint` skill now. The user wants to START a loop. Pass the task description (everything after `/man-loop-start`) to the skill. Tell the skill the intent is "start".
