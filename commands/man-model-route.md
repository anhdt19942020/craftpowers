---
description: "Route a task to the right Claude model by complexity. Prevents overspending on simple tasks."
---

Invoke the `model-route` skill now. Pass the full task description (everything after `/man-model-route`) to the skill.
