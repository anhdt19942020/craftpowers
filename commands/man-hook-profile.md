---
description: "View or switch the active hook profile (minimal/standard/strict). Controls which quality gates run."
---

Invoke the `hook-profile` skill now. Pass any argument after `/man-hook-profile` (e.g., `minimal`, `standard`, `strict`) directly to the skill.
