---
description: "Inspect current loop iteration status and history. Shows pass/fail count and whether loop is done."
---

Invoke the `loop-checkpoint` skill now. The user wants to check STATUS. Pass any arguments after `/man-loop-status` (e.g., `record pass`, `record fail`) to the skill.
