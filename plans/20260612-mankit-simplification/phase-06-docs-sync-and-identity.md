# Phase 06 — Docs Sync + Identity Fix

## Context Links
- Parent: [plan.md](plan.md)
- Depends: Phases 01–05 complete (docs describe final state)

## Overview
- **Priority:** Low | **Status:** ✅ Done | **Risk:** Low
- README/CLAUDE.md drift severe. README claims 38 skills / 10 agents / 4 hooks; post-simplification reality will be ~50 skills / ~10 agents / re-tiered hooks / ~14 commands. CLAUDE.md is the upstream **Superpowers contributor guidelines** verbatim — wrong identity for this fork.

## Key Insights
- CLAUDE.md talks about Superpowers PR policy, "94% PR rejection rate", upstream maintainer notes — irrelevant and misleading for agents working in THIS repo. Replace with mankit-own guide: repo layout, versioning rule (plugin.json + marketplace.json bump together — note 6.60.x had a missing-comma hotfix, so add validate step), eval workflow, release flow.
- `docs/mankit/` (11 specs + 9 plans) is **gitignored** — design knowledge invisible to clones. Decision needed (unresolved Q3): un-ignore (version it) or move to `docs/` tracked area. Recommend: un-ignore specs at minimum.
- `.gitignore` also ignores `tests/test_dispatch.py` — a test file. Either fix and track it, or delete it; ignoring a test is a smell.
- README counts should be generated, not hand-written: tiny script (`scripts/`) counts skills/agents/commands/hooks and patches README — kills drift permanently (ck principle: machine holds state).

## Requirements
- All counts in README generated by script, runnable in CI/pre-release.
- CLAUDE.md ≤ 60 lines, mankit-specific.

## Related Code Files
- Modify: `README.md`, `CLAUDE.md`, `.gitignore`
- Create: `scripts/sync-readme-counts.py` (or extend `bump-version.sh`)
- Decide: `docs/mankit/` tracking, `tests/test_dispatch.py`

## Implementation Steps
1. Write count-sync script; wire into release flow (bump-version.sh).
2. Rewrite CLAUDE.md: mankit identity, repo map, version-bump rule + plugin.json validate step, eval/release workflow.
3. Resolve docs/mankit gitignore decision with owner; apply.
4. Fix or delete `tests/test_dispatch.py`; update `.gitignore`.
5. Regenerate README counts; manual read-through of README workflow section vs actual commands.

## Todo List
- [ ] Count-sync script
- [ ] CLAUDE.md rewrite
- [ ] docs/mankit decision applied
- [ ] test_dispatch.py resolved
- [ ] README regenerated + reviewed

## Success Criteria
- README counts == script output == reality
- CLAUDE.md contains zero Superpowers-upstream policy text
- No test files in .gitignore

## Risk Assessment
- Count script regex brittle → keep it dumb: count dirs/files, no md parsing.

## Security Considerations
- None.

## Next Steps
- Tag release (minor bump), update project changelog. Plan complete.
